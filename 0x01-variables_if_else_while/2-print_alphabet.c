#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - print a-z using putchar
 *
 * Return: 0 (Success)
 */
int lower_y(tolower(y)
		{
		return tolower(y);
int main(void) 
{
	for (int y = 'A'; y <= 'Z'; y++)
	{
		int lower_y = tolower(y);
		putchar(lower_y);
	}
	return (0);
}
